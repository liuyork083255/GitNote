如果是发送 非原始类型，这时候可以用：
	session.createObjectMessage();
	
	一般我们是用MapMessage这个来，但是本案例这种配置不能用这个类来发送非原始类，即使这个类实现了序列化接口
	
	在使用ObjectMessage这个类的时候，如果发送非原始类型，则要调用setObject方法，参数要求实现序列化接口，所以
	如果发送什么类型，只要将这些属性封装在一个实现了序列化接口的类里面即可
















